spring:
  config:
    activate:
      on-profile: prod

  jpa:
    hibernate:
      ddl-auto: none

  security:
    oauth2:
      client:
        registration:
          github:
            client-id: ENC(KUlAC4d/1CvSfwuMk+kwhUDkuXgI+LVhrKNiCp3Ym8A=)
            client-secret: ENC(rRDH2gxZJXv/GENPAWfpQnNwEohZpG/oPq1rViX98NflQbHK2UnMlUhqlIdQ+3NhaM0BSR7/wfk=)
            redirect-uri: '{baseUrl}/api/v1/login/oauth2/{registrationId}'
            scope: user, email

logging:
  level:
    com.zaxxer.hikari.HikariConfig: DEBUG
    com.zaxxer.hikari: TRACE

decorator:
  datasource:
    p6spy:
      enable-logging: false