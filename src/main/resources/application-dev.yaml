spring:
  config:
    activate:
      on-profile: dev

  jpa:
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: create

decorator:
  datasource:
    p6spy:
      enable-logging: true
